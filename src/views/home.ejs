<main>
  <h1 class="page-title">פורומים</h1>
  <h4>
    תפילה תשובה וצדקה מעבירים את רוע הגזירה - בראשית רבה &nbsp;
    <img src="/forum/logo.svg" alt="" class="logo" />
  </h4>
  <a href="/forum/new-posts" class="btn-latest">🆕 פוסטים אחרונים</a>

  <% if (!forums || forums.length === 0) { %>
  <p class="empty">לא קיימים פורומים.</p>
  <% } else { %>
  <ul class="thread-list">
    <% forums.forEach(f => { %>
    <li>
      <a class="thread-title" href="/forum/f/<%= f.id %>"> <%= f.name %> </a>

      <% if (f.description) { %>
      <div class="meta"><%= f.description %></div>
      <% } %>

      <a class="thread-title" href="/forum/f/<%= f.id %>"> כניסה לפורום → </a>
    </li>
    <% }) %>
  </ul>
  <% } %>
</main>
